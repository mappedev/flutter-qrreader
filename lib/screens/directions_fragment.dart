import 'package:flutter/material.dart';

class DirectionsFragment extends StatelessWidget {
  const DirectionsFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Directions Fragment'),
    );
  }
}
