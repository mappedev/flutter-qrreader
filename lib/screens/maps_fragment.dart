import 'package:flutter/material.dart';

class MapsFragment extends StatelessWidget {
  const MapsFragment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Maps Fragment'),
    );
  }
}
